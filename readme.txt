Each of the classes has setters and getters

There is an interface to check for availability

The prouct class calls the read category method of the database based on its category name
then it calls the add product method of the category it falls under

The category class has an arraylist for storing all the products that fall under each category object
It has a method for adding objects to the list (called from the object class)
It has a method for displaying all the products of a category object